// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Cambia esto por tu dominio real cuando despliegues el sitio.
  // Es necesario para que las imágenes compartidas en WhatsApp y redes sociales funcionen.
  site: 'https://menu-scan-la-diabla-comas.vercel.app/',
});
